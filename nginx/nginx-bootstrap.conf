server {
    listen 80;
    server_name scalefybd.com www.scalefybd.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 200 'bootstrapping...';
        add_header Content-Type text/plain;
    }
}
